# ===== 基本 =====
CONFIG_INPUT=y
CONFIG_SPI=y
CONFIG_PMW3610=y
CONFIG_ZMK_POINTING=y
CONFIG_ZMK_EXT_POWER=y
CONFIG_ZMK_SLEEP=y
# 必要なければ外す（非推奨警告回避）
# CONFIG_NFCT_PINS_AS_GPIOS=y

# ===== PMW3610（左手）=====
CONFIG_PMW3610_INVERT_Y=n
CONFIG_PMW3610_SWAP_XY=y
# CONFIG_PMW3610_INVERT_X=y
CONFIG_PMW3610_REPORT_INTERVAL_MIN=3
CONFIG_PMW3610_RUN_DOWNSHIFT_TIME_MS=2000
CONFIG_PMW3610_REST1_SAMPLE_TIME_MS=15
CONFIG_PMW3610_REST3_SAMPLE_TIME_MS=250

# ===== BLE（Peripheral再接続短縮）=====
CONFIG_BT_GAP_AUTO_UPDATE_CONN_PARAMS=y

# 7.5ms（6 * 1.25ms）
# Kconfigは=の右に数字だけ。コメントは別行で書く
CONFIG_BT_PERIPHERAL_PREF_MIN_INT=6

# 15ms（12 * 1.25ms）
CONFIG_BT_PERIPHERAL_PREF_MAX_INT=12

CONFIG_BT_PERIPHERAL_PREF_LATENCY=0
CONFIG_BT_PERIPHERAL_PREF_TIMEOUT=400

# ボード/SDKが許せば拡張広告ON（無くてもビルド可）
CONFIG_BT_EXT_ADV=y

# Peripheral側でUSB/スムーススクロールは依存を満たさないので明示的に切る
CONFIG_ZMK_POINTING_SMOOTH_SCROLLING=n
CONFIG_ZMK_USB=n

# スリープ猶予（頻繁に寝て広告停止→再広告の遅延を避ける）
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=60000
# CONFIG_ZMK_DEEP_SLEEP_IDLE_TIMEOUT=300000
